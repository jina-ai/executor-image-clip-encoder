manifest_version: 1
name: CLIPImageEncoder
description: Image encoder that wraps the image embedding functionality using the CLIP model
url: https://github.com/jina-ai/executor-image-clip-encoder
keywords: [clip, image, crossmodal, encoder, PyTorch, en]
